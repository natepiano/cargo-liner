# Testing reduction coverage ledger

Every removal, merge, matrix shrink, dropped compile configuration, move, or rewritten timing assertion must preserve the same behavior path, assertion strength, platforms, and features. Missing assertions transfer to the named survivor before removal; scenarios without equivalent coverage move to unit tests. Each phase reconciles this ledger against its diff and the before/after nextest inventory. Timing changes retain lower and upper bounds with documented CI scheduling headroom.

| Item | Kind | File:line before | Behavior path | Now asserted by | Platforms and features | CI job |
| --- | --- | --- | --- | --- | --- | --- |
| a_second_holder_times_out_with_an_actionable_fact_free_error | assertion | `crates/cargo-berth/src/ledger/lock.rs:155` | Contended descriptor acquire reaches `AcquisitionTimedOut`; 100 ms retry budget → zero nonblocking budget | `crates/cargo-berth/src/ledger/lock.rs:237`: retained error variant, wait/retry advice, no delete advice, no lock path; resolver tests separately cover duration selection | Existing unit-test platforms/features; no cfg narrowing | Test Suite |
| `lock_contention_is_retryable_while_corrupt_journal_is_unreadable` | assertion | `crates/cargo-berth/tests/lifecycle.rs:853` | init and release retain contention JSON/status 6 and retry guidance; corrupt ledger still returns 4; implicit 10 s each, no elapsed bounds → each ≥300 ms and <5300 ms | `crates/cargo-berth/tests/lifecycle.rs:860`: same behavioral assertions plus readiness signal and both elapsed bounds | Linux/macOS existing Unix integration targets; debug assertions; default/all features | Test Suite |
| `post_commit_reports_corrupt_ledger_and_lock_deadline_without_removing_the_commit` | assertion | `crates/cargo-berth/tests/drift.rs:3481` | managed post-commit hook keeps the commit and reports lock deadline/retry guidance; corrupt-ledger assertions retained; implicit 10 s, no elapsed bounds → ≥300 ms and <5300 ms | `crates/cargo-berth/tests/drift.rs:3488`: same behavioral assertions plus readiness signal and both elapsed bounds | Linux/macOS existing Unix integration targets; debug assertions; default/all features | Test Suite |
| `a_contended_ledger_states_the_engine_message` | assertion | `crates/cargo-berth/tests/hooks.rs:321` | pre-tool-use preserves engine-authored refusal and busy-operation text; implicit 10 s, no elapsed bounds → ≥300 ms and <5300 ms | `crates/cargo-berth/tests/hooks.rs:327`: same behavioral assertions plus readiness signal and both elapsed bounds | Linux/macOS existing Unix integration targets; debug assertions; default/all features | Test Suite |
| `session_start_states_the_ledger_contention_presentation` | assertion | `crates/cargo-berth/tests/hooks.rs:3023` | session-start preserves contention summary and single 10-second retry-budget wording; implicit 10 s, no elapsed bounds → ≥300 ms and <5300 ms | `crates/cargo-berth/tests/hooks.rs:3076`: same behavioral assertions plus readiness signal and both elapsed bounds | Linux/macOS existing Unix integration targets; debug assertions; default/all features | Test Suite |
| `rewritten_integration_reachability_runs_under_the_mutation_lock` | assertion | `crates/cargo-berth/tests/liveness.rs:928` | claim waits behind paused resolution, returns contention/status 6; resolution validation remains asserted; implicit 10 s, no elapsed bounds → ≥300 ms and <5300 ms | `crates/cargo-berth/tests/liveness.rs:934`: same behavioral assertions plus readiness signal and both elapsed bounds | Linux/macOS existing Unix integration targets; debug assertions; default/all features | Test Suite |
| `identity_clear_session_reports_mutation_lock_contention` | assertion | `crates/cargo-berth/tests/liveness.rs:974` | identity clear-session retains session identity and returns contention/status 6 behind paused resolution; implicit 10 s, no elapsed bounds → ≥300 ms and <5300 ms | `crates/cargo-berth/tests/liveness.rs:983`: same behavioral assertions plus readiness signal and both elapsed bounds | Linux/macOS existing Unix integration targets; debug assertions; default/all features | Test Suite |
| `hook_lock_contention_uses_one_shortened_lock_deadline` | assertion | `crates/cargo-berth/tests/gate.rs:2470` | private reference-transaction reports lock-specific deadline, exit 6, 10-second wording and bypass recovery; excludes total-deadline diagnostic; ≥9 s and <15 s → ≥300 ms and <5300 ms | `crates/cargo-berth/tests/gate.rs:2479`: same behavioral assertions plus readiness signal and both elapsed bounds | Linux/macOS existing Unix integration targets; debug assertions; default/all features | Test Suite |
| Watcher lifetime waits | assertion | `crates/cargo-port/src/watcher/runtime.rs:1307`, `:1315` | Guard survives work in watcher_loop and drops at shutdown; two 600 ms sleeps → dynamic-registration signal plus thread join | `crates/cargo-port/src/watcher/runtime.rs:1236`, spawn_watcher_thread_keeps_watcher_guard_alive_until_shutdown: not dropped while loop registration is blocked; dropped after join | Linux/macOS/Windows; unchanged features | Test Suite; Windows Build Check (compile) |
| Cached startup lint absence window | assertion | `crates/cargo-port/src/lint/runtime/supervisor.rs:1128` | Sync hydrates cached Passed without scheduling discovery lint; 1 s absence window → worker initialization and join | `crates/cargo-port/src/lint/runtime/supervisor.rs:1168`, sync_projects_hydrates_terminal_cache_without_running_discovery_lint: actual initial worker schedule is Idle; joined supervisor messages contain cached Passed and no LintStatus | Linux/macOS/Windows; unchanged features | Test Suite; Windows Build Check (compile) |
| Uncached startup lint absence window | assertion | `crates/cargo-port/src/lint/runtime/supervisor.rs:1192` | Immediate discovery config still defers uncached sync lint; 500 ms absence window → worker initialization and join | `crates/cargo-port/src/lint/runtime/supervisor.rs:1236`, sync_projects_defers_lint_even_when_immediate_and_uncached: actual initial schedule is Idle; joined supervisor messages contain NoLog and no LintStatus | Linux/macOS/Windows; unchanged features | Test Suite; Windows Build Check (compile) |
| Lint-registration trio shared fixture and completion wait | move | `crates/cargo-port/src/tui/app/mod.rs:15203`, `:15262`, `:15326` | Ordinary discovery, arbitrary named linked worktree via primary filter, and stale-discovery refresh retain all three tests; shared owning TestApp fixture and channel receive replace polling sleeps; 150 ms absence window → initial Idle schedule observation | `crates/cargo-port/src/tui/app/mod.rs:15250`, `:15274`, `:15315`: discovery/refresh assertions retained; expect_registered_project_lints_after_edit at `:15220` requires Passed for each edited project; linked case additionally requires NoLog after startup notification | Linux/macOS/Windows; unchanged features | Test Suite; Windows Build Check (compile) |
| Git fixture setup consolidation | move | `crates/cargo-port/src/watcher/runtime.rs:2916`; `crates/cargo-port/src/tui/app/mod.rs:16200`, `:16233` | Existing fixture files become a committed repository; common init/add/commit replaces separate identity-config subprocesses in package, bare repo, and workspace-member fixtures | `crates/cargo-port/src/test_support.rs:43`, init_git_repo asserts success of all three git commands; original fixture callers and assertions remain | Linux/macOS/Windows; unchanged features | Test Suite; Windows Build Check (compile) |
| Timing-print watcher test | test | `crates/cargo-port/src/watcher/runtime.rs:2879` | register_watch_roots_reports_elapsed_for_representative_roots only initialized a real watcher and printed elapsed time; no assertions before deletion | `crates/cargo-port/src/watcher/runtime.rs:957`, startup_registration_introduces_no_overlapping_watches asserts successful registration and root count for two representative roots; `:667`, register_watch_roots_reports_per_root_failures asserts successful roots plus both failure kinds. No timing assertion existed to transfer | Linux/macOS/Windows; unchanged features | Test Suite; Windows Build Check (compile) |

Phase 1 source-diff reconciliation: all existing behavior tests remain except `register_watch_roots_reports_elapsed_for_representative_roots`, which had no assertions. The gate contention test was renamed and retained; one outer-timeout integration test and three debug unit tests were added. Two release-only unit tests are compiled out in the debug run. Final scoped nextest counts are berth 634 and port 1170, with zero skipped in both. The subsequently authorized nextest-list inventories confirm berth 630 → 634 and port 1171 → 1170, exactly matching the documented additions, rename, and deletion. Both release-only resolver tests also passed. The full-workspace repeat gate remains pending; see the measurements document.

The additive outer gate test at `crates/cargo-berth/tests/gate.rs:2495` asserts ≥1000 ms and <6000 ms, the total-deadline-specific message, exit 6, and bypass guidance. Existing git fixture setup remains outside elapsed measurement. No berth tests or assertions were deleted.

## Phase 2

| Item | Kind | File:line before | Behavior path | Now asserted by | Platforms and features | CI job |
| --- | --- | --- | --- | --- | --- | --- |
| `cli_rejects_unknown_process_arguments` | move | `crates/cargo-tile/tests/summary_totals.rs:322` | cli rejects unknown process arguments | `crates/cargo-tile/src/cli.rs:487`: child exits 2 and stderr rejects --exact; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `promoted_totals_count_each_descendant_once_and_exclude_driver_and_sibling` | move | `crates/cargo-tile/tests/summary_totals.rs:346` | promoted totals count each descendant once and exclude driver and sibling | `crates/cargo-tile/src/census/summary_totals_tests.rs:248`: promoted CPU is 7% and 48%, excluding driver and sibling contributions; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `zero_own_ticks_across_thirteen_samples_keep_promoted_descendant_cpu_numeric` | move | `crates/cargo-tile/tests/summary_totals.rs:359` | zero own ticks across thirteen samples keep promoted descendant cpu numeric | `crates/cargo-tile/src/census/summary_totals_tests.rs:261`: zero-own-tick samples keep promoted descendant totals numeric across thirteen samples; original assertions retained | Linux only; unchanged features | Test Suite |
| `unreadable_parent_counter_keeps_its_promoted_total_unavailable_despite_busy_leaf` | move | `crates/cargo-tile/tests/summary_totals.rs:383` | unreadable parent counter keeps its promoted total unavailable despite busy leaf | `crates/cargo-tile/src/census/summary_totals_tests.rs:285`: unreadable parent remains unavailable despite a busy descendant; original assertions retained | Linux only; unchanged features | Test Suite |
| `command_rows_keep_their_own_attributions_and_the_lead_keeps_its_group_total` | move | `crates/cargo-tile/tests/summary_totals.rs:402` | command rows keep their own attributions and the lead keeps its group total | `crates/cargo-tile/src/census/summary_totals_tests.rs:304`: command rows retain per-process attribution while the lead retains the group total; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `an_unavailable_contribution_affects_only_its_promoted_subtree` | move | `crates/cargo-tile/tests/summary_totals.rs:430` | an unavailable contribution affects only its promoted subtree | `crates/cargo-tile/src/census/summary_totals_tests.rs:332`: unavailable readings affect only their promoted subtree and preserve other totals; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `an_unavailable_hidden_driver_does_not_replace_known_promoted_totals` | move | `crates/cargo-tile/tests/summary_totals.rs:487` | an unavailable hidden driver does not replace known promoted totals | `crates/cargo-tile/src/census/summary_totals_tests.rs:389`: unavailable hidden driver preserves known promoted totals; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `a_missing_nested_attribution_is_unavailable_instead_of_zero` | move | `crates/cargo-tile/tests/summary_totals.rs:519` | a missing nested attribution is unavailable instead of zero | `crates/cargo-tile/src/census/summary_totals_tests.rs:421`: missing nested attribution is unavailable, not measured zero; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `a_non_process_contribution_with_a_cpu_bucket_keeps_leads_unavailable` | move | `crates/cargo-tile/tests/summary_totals.rs:550` | a non process contribution with a cpu bucket keeps leads unavailable | `crates/cargo-tile/src/census/summary_totals_tests.rs:452`: non-process CPU contribution leaves lead totals unavailable; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `a_shared_pid_under_distinct_invocations_cannot_publish_a_double_charged_total` | move | `crates/cargo-tile/tests/summary_totals.rs:599` | a shared pid under distinct invocations cannot publish a double charged total | `crates/cargo-tile/src/census/summary_totals_tests.rs:501`: shared PID in distinct invocations cannot publish double-charged totals; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `an_unproven_group_lead_cannot_use_its_cpu_bucket_for_a_subtree_total` | move | `crates/cargo-tile/tests/summary_totals.rs:659` | an unproven group lead cannot use its cpu bucket for a subtree total | `crates/cargo-tile/src/census/summary_totals_tests.rs:561`: unproven group lead cannot publish a CPU bucket as its subtree total; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `measured_zero_remains_available_in_each_promoted_subtree` | move | `crates/cargo-tile/tests/summary_totals.rs:704` | measured zero remains available in each promoted subtree | `crates/cargo-tile/src/census/summary_totals_tests.rs:606`: measured zero remains numeric in each promoted subtree; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `subtree_totals_round_once_after_summing_raw_attributions` | move | `crates/cargo-tile/tests/summary_totals.rs:731` | subtree totals round once after summing raw attributions | `crates/cargo-tile/src/census/summary_totals_tests.rs:633`: subtree totals sum raw attributions before rounding once; original assertions retained | Linux/macOS; unchanged features | Test Suite; macOS cargo-tile |
| `every_non_owner_write_permission_on_root_allows_owned_file_cleanup` | move | `crates/cargo-tile/tests/capture_root_acl.rs:218` | every non owner write permission on root allows owned file cleanup | `crates/cargo-tile/src/root_scan/sweep_authority.rs:938`: for all eight directory write permissions, removes both owned files with two budget units and no skips; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `every_non_owner_write_permission_on_state_allows_owned_file_cleanup` | move | `crates/cargo-tile/tests/capture_root_acl.rs:223` | every non owner write permission on state allows owned file cleanup | `crates/cargo-tile/src/root_scan/sweep_authority.rs:943`: for all eight state-directory write permissions, removes both owned files with two budget units and no skips; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `every_non_owner_write_permission_on_pids_allows_owned_file_cleanup` | move | `crates/cargo-tile/tests/capture_root_acl.rs:228` | every non owner write permission on pids allows owned file cleanup | `crates/cargo-tile/src/root_scan/sweep_authority.rs:948`: for all eight pids-directory write permissions, removes both owned files with two budget units and no skips; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `non_owner_write_entry_after_read_only_entry_allows_owned_file_cleanup` | move | `crates/cargo-tile/tests/capture_root_acl.rs:233` | non owner write entry after read only entry allows owned file cleanup | `crates/cargo-tile/src/root_scan/sweep_authority.rs:953`: read-only ACL entry before write entry still permits both owned unlinks; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `non_owner_write_added_after_open_allows_owned_file_cleanup` | move | `crates/cargo-tile/tests/capture_root_acl.rs:244` | non owner write added after open allows owned file cleanup | `crates/cargo-tile/src/root_scan/sweep_authority.rs:964`: write ACL granted after opening still permits both owned unlinks; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `non_owner_write_added_by_first_sweep_callback_allows_both_unlinks` | move | `crates/cargo-tile/tests/capture_root_acl.rs:255` | non owner write added by first sweep callback allows both unlinks | `crates/cargo-tile/src/root_scan/sweep_authority.rs:975`: first callback grants ACL; at least two callbacks, both files removed, budget reduced by two; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `non_owner_write_added_before_registration_unlink_allows_both_unlinks` | move | `crates/cargo-tile/tests/capture_root_acl.rs:276` | non owner write added before registration unlink allows both unlinks | `crates/cargo-tile/src/root_scan/sweep_authority.rs:996`: ACL grant occurs after log removal; registration removed, owner retained, budget reduced by two; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `directories_without_acls_still_sweep` | move | `crates/cargo-tile/tests/capture_root_acl.rs:305` | directories without acls still sweep | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1025`: absent ACL permits both unlinks with no skips; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `directories_with_explicitly_emptied_acls_still_sweep` | move | `crates/cargo-tile/tests/capture_root_acl.rs:311` | directories with explicitly emptied acls still sweep | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1031`: explicit zero-entry ACL permits both unlinks with no skips; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `read_only_non_owner_acl_on_each_directory_still_sweeps` | move | `crates/cargo-tile/tests/capture_root_acl.rs:323` | read only non owner acl on each directory still sweeps | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1043`: read-only non-owner ACL on each ancestor permits both unlinks; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `read_only_non_owner_acl_added_after_open_still_sweeps` | move | `crates/cargo-tile/tests/capture_root_acl.rs:338` | read only non owner acl added after open still sweeps | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1058`: read-only ACL added after opening permits both unlinks; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `owner_write_acl_on_each_directory_still_sweeps` | move | `crates/cargo-tile/tests/capture_root_acl.rs:351` | owner write acl on each directory still sweeps | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1071`: owner write ACL on each ancestor permits both unlinks; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `non_owner_deny_entry_is_not_a_write_grant` | move | `crates/cargo-tile/tests/capture_root_acl.rs:371` | non owner deny entry is not a write grant | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1091`: non-owner deny entry permits owned cleanup with no skips; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `foreign_registration_in_acl_writable_directory_preserves_pair` | move | `crates/cargo-tile/tests/capture_root_acl.rs:383` | foreign registration in acl writable directory preserves pair | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1103`: foreign registration ownership retains pair, counts skipped attempts, removes zero files; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `foreign_log_in_acl_writable_directory_preserves_pair` | move | `crates/cargo-tile/tests/capture_root_acl.rs:396` | foreign log in acl writable directory preserves pair | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1116`: foreign log ownership retains pair, counts skipped attempts, removes zero files; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `writable_directory_mode_bits_allow_owned_file_cleanup` | move | `crates/cargo-tile/tests/capture_root_acl.rs:409` | writable directory mode bits allow owned file cleanup | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1129`: group/other directory write bits on each ancestor permit owned cleanup; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `writable_file_mode_bits_preserve_pair_in_acl_writable_directory` | move | `crates/cargo-tile/tests/capture_root_acl.rs:426` | writable file mode bits preserve pair in acl writable directory | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1146`: group/other file write bits on either candidate retain pair and count skips; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `multiply_linked_file_preserves_pair_in_acl_writable_directory` | move | `crates/cargo-tile/tests/capture_root_acl.rs:444` | multiply linked file preserves pair in acl writable directory | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1164`: second hard link retains pair and preserves alias contents; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `symlinked_file_preserves_target_in_acl_writable_directory` | move | `crates/cargo-tile/tests/capture_root_acl.rs:460` | symlinked file preserves target in acl writable directory | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1180`: symlink candidate retains pair, symlink and unchanged target contents; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `registration_replaced_before_its_unlink_survives_in_acl_writable_directory` | move | `crates/cargo-tile/tests/capture_root_acl.rs:483` | registration replaced before its unlink survives in acl writable directory | `crates/cargo-tile/src/root_scan/sweep_authority.rs:1203`: registration replaced after log removal survives with replacement contents and bounded budget; original assertions retained | macOS; unchanged features | macOS cargo-tile |
| `generated_contract_retains_every_closed_selector_branch` | move | `crates/cargo-berth/tests/output_contract.rs:20` | generated contract retains every closed selector branch | `crates/cargo-berth/src/output_contract.rs:210`: checked-in contract retains exactly already_held/appended/widened and file/tree selectors, including cardinality and per-branch const constraints; original assertions retained | Linux/macOS; unchanged features | Test Suite |
| shim_registration integration binary and support modules | move | `crates/cargo-tile/tests/shim_registration.rs:1`; `crates/cargo-tile/tests/support/shared_capture.rs:1`; `crates/cargo-tile/tests/support/rows_readout.rs:1` | 123 shim publication, reader, shared capture/installer and rows-readout tests; eliminate 732 duplicate crate unit-test executions | `crates/cargo-tile/src/shim_registration/mod.rs:20` (reader/CLI children and tests module), `crates/cargo-tile/src/shim_registration/wire.rs:1` (24 wire tests), `crates/cargo-tile/src/shim_registration/shared_capture.rs:1` (38 tests), `crates/cargo-tile/src/shim_registration/rows_readout.rs:1` (2 tests): every original assertion retained under shim_registration module paths | Existing Linux/macOS cfg gates; default/all features; test-only modules | Test Suite; macOS cargo-tile |
| Shim wire birth-time settle: `versioned_fields_preserve_original_argument_bytes`, `one_argument_with_a_space_differs_from_two_arguments`, `matching_relative_directories_keep_their_absolute_paths_and_writer_homes`, `raw_directory_preserves_a_trailing_newline`, `registration_log_and_live_fifo_share_pid_and_generation`, `stale_invocation_fifo_still_publishes_registration_and_captures_stderr`, `fifo_removal_failure_preserves_original_cargo_and_unowned_directory`, `publication_links_a_complete_tmp_file_into_place`, `birth_stamp_belongs_to_the_pid_in_the_registration_filename`, `same_pid_and_calendar_second_receive_different_generations`, `publication_after_final_confirmation_survives_delayed_unlinks`, `darwin_birth_conversion_scopes_locale_and_keeps_cargo_environment`, `darwin_birth_conversion_removes_ps_column_padding`, `darwin_birth_conversion_falls_back_to_gnu_date`, `darwin_birth_conversion_distinguishes_both_sides_of_dst_fallback`, `failed_darwin_birth_conversion_publishes_empty_identity_field`, `occupied_registration_runs_original_cargo_without_replacing_owned_files`, `occupied_registration_also_preserves_its_existing_log`, `occupied_directory_is_a_setup_failure_and_keeps_its_contents`, `term_after_publication_cleans_artifacts_without_starting_cargo`, `dead_pid_captures_are_reaped_before_publication_without_touching_another_account`, `same_pid_predecessor_is_reaped_before_the_new_invocation_registers`, `legacy_registration_and_log_survive_a_new_shim_run`, `phase_two_records_and_malformed_neighbors_survive_publication_and_cleanup` | assertion | `crates/cargo-tile/tests/shim_registration.rs:2063`; wire tests `:2934`–`:3753` | Explicit per-test option retains the original 1 s sleep for 3 native birth-comparison tests and skips it for the remaining 21; controlled Darwin conversions use deterministic birth timestamps | `crates/cargo-tile/src/shim_registration/wire.rs:503` `versioned_fields_preserve_original_argument_bytes`: fields[..8] equals the expected version, generation, native boot, independently observed shim birth, log, directory, home and argument count; argument bytes unchanged; `crates/cargo-tile/src/shim_registration/wire.rs:798` `birth_stamp_belongs_to_the_pid_in_the_registration_filename`: registration filename PID equals independently recorded shim PID; fields[3] equals independently observed nonempty numeric native birth; boot is nonempty; `crates/cargo-tile/src/shim_registration/wire.rs:820` `same_pid_and_calendar_second_receive_different_generations`: repeated invocations share PID, calendar value, boot and shell birth; generation, log and registration names differ; all 24 original test bodies and assertions preserved apart from constructor options | Linux and macOS; unchanged features and platform gates; native macOS ps lstart separation retained | Test Suite; macOS cargo-tile |

The owned small moves preserve all 34 test bodies and their assertions: 13 summary/CLI tests, 20 macOS ACL tests, and one berth selector test. Complete ACL module and summary fixture/helper contents were compared with the pre-phase source after whitespace and import normalization. The CLI subprocess now selects `cli::tests::cli_rejects_unknown_process_arguments` with `--exact`; its exit-code and stderr assertions are unchanged.

Source counts correct the work-order estimates: summary contains 13 tests including CLI (two Linux-only), and ACL contains 20 tests. Thus the small moves add 13 bin tests on Linux and 31 on macOS, not 14 and 36. No scenario was deleted to produce these counts. Berth retains all 633 distinct tests, with exactly the output-contract selector test changing target and module path.

Phase 2 Linux inventory reconciliation: tile bin 732 → 868, summary 745 → absent, shim 855 → absent, cli_lifecycle 8 → 8, shim_modes 20 → 20. Exactly 1464 copied unit-test entries disappear; all 896 distinct tests remain under their mapped names. The macOS ACL integration target is absent from the final source tree; its 20 unique tests retain their macOS gate in `sweep_authority::acl_tests`. Berth bin grows 224 → 225, its output_contract target disappears, and total remains 633. All 36 phase rows reconcile to the moves and the per-test wire sleep change.

Final native macOS verification passed 908/908 tests with zero skipped and passed clippy after all writers posted done. All 20 relocated ACL tests ran in `root_scan::sweep_authority::acl_tests`; the three native wire birth-comparison cases also passed. Final Linux tile and berth scoped gates passed 896/896 and 633/633 respectively, with zero skipped, and both lint gates passed. Full-workspace repeats remain pending as documented in the measurements file.
